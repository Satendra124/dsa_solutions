#include <bits/stdc++.h>
using namespace std;
int mod = 1e9+7;
#define fast ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
typedef long long int ll;

string a = "";
string b = "";
string bits;
int n;

pair<string,string> process(int i){

}

int main()
{
    fast;
    int t;
    cin>>t;
    while (t--)
    {
        cin>>n;
        cin>>bits;
        pair<string,string> ab = process(0);
        cout<<ab.first<<endl;
        cout<<ab.second<<endl;
    }
    return 0;
}